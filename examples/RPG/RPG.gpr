project RPG extends "../../geste" is

   for Source_Dirs use ("src/**", "../utils/**");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("main.adb");

   package Linker is
      for Default_Switches ("Ada") use ("-lSDL");
  end Linker;

end RPG;
