project GESTE is

  for Languages use ("Ada");
  for Source_Dirs use ("src", "src/fonts");
  for Object_Dir use "obj";

   package Compiler is
       --  Suppress warnings on bad fixed values
      for Default_Switches ("Ada") use ("-gnatwB");
   end Compiler;
end GESTE;
